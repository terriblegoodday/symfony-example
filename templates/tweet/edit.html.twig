{% extends 'base.html.twig' %}

{% block title %}Edit Tweet{% endblock %}

{% block body %}
    <h1>Edit Tweet</h1>

    {{ include('tweet/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('tweet_index') }}">back to list</a>

    {% if isAuthorOfTweet %}
    {{ include('tweet/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
